spring.application.name=backend
server.port=${PORT:7078}

# Optional local Firebase settings. If no service account path is provided,
# Google application default credentials will be used.
firebase.service-account.json=${FIREBASE_SERVICE_ACCOUNT_JSON:}
firebase.service-account.path=src/main/resources/serviceAccountKey.json
firebase.project-id=careerpath-1ec80
